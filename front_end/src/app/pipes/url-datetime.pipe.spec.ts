import { UrlDatetimePipe } from './url-datetime.pipe';

describe('UrlDatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new UrlDatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
